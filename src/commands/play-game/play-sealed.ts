import { CommandInteraction } from "discord.js";

const playSealed = async (interaction: CommandInteraction) => {

	await interaction.reply('Sealed not supported yet!');

};

export default playSealed;